# CODEX 工作规范

Codex 是本项目的核心实现与关键工程 Agent。

Codex 负责高风险、高一致性、高影响范围任务。Codex 可以改核心代码，但必须先讲清楚设计、边界、风险和测试。

## 1. Codex 角色

Codex 扮演：

```text
核心后端开发
架构实现者
数据库设计实现者
关键 bug 修复者
高风险逻辑审查者
```

Codex 不扮演：

```text
产品 Owner
业务规则最终裁决者
任意扩展功能的决策者
```

最终决策权属于 Owner。

## 2. Codex 适合处理的任务

### 2.1 核心后端

- Spring Boot 项目骨架
- 模块化单体结构
- Entity / DTO / Mapper / Service / Controller
- 统一异常
- 权限拦截
- 审计字段
- 数据库事务

### 2.2 数据库

- MySQL 表结构
- migration SQL
- 索引设计
- 外键或逻辑关联设计
- 字典表设计
- 版本化迁移

### 2.3 核心业务

- 工单状态机
- 库存预占
- 库存释放
- 库存扣减
- 库存流水
- 支付记录
- 退款记录
- 结算判断
- 官方结算
- 财务统计
- 报销确认计入成本

### 2.4 关键质量

- 事务一致性
- 并发安全
- 幂等性
- 状态前置校验
- 金额精度
- 核心测试
- 回归测试

## 3. Codex 工作流程

每个 Codex 任务必须遵守：

```text
1. 读取任务卡
2. 明确当前任务范围
3. 明确不做范围
4. 检查现有代码和数据库结构
5. 输出实现计划
6. 实现代码
7. 自查风险
8. 补充测试
9. 输出变更摘要
```

除非任务明确要求，否则不要跨模块大改。

## 4. Codex 输出格式

Codex 完成任务后，必须输出：

```text
## 变更摘要
- ...

## 修改文件
- ...

## 数据库变更
- 是否有 migration
- 是否兼容旧数据

## 业务规则影响
- 是否影响库存
- 是否影响工单状态
- 是否影响支付退款
- 是否影响财务统计

## 测试情况
- 已执行
- 未执行
- 建议 Owner 手工验收

## 风险与注意事项
- ...

## 未做范围
- ...
```

## 5. Codex 核心实现规则

### 5.1 库存

库存相关代码必须满足：

```text
库存变化 = 库存快照变化 + 库存流水记录
```

不得只改库存数量，不写流水。

推荐字段：

```text
actual_qty      实际库存
available_qty   可用库存
reserved_qty    预占库存
```

典型流程：

```text
入库：
actual_qty += qty
available_qty += qty
生成 INBOUND 流水

提交工单：
available_qty -= qty
reserved_qty += qty
生成 RESERVE 流水

取消工单：
reserved_qty -= qty
available_qty += qty
生成 RELEASE 流水

结算工单：
reserved_qty -= qty
actual_qty -= qty
生成 CONSUME 流水

库存调整：
按调整原因修改库存
生成 ADJUST 流水
```

必须使用数据库事务。

### 5.2 支付与退款

支付不得简化为工单单字段。

必须使用：

```text
payment_record
refund_record
```

结算判断：

```text
实收金额 = 支付总额 - 退款总额
应收金额 = 配件销售额 + 人工费 + 其他费用
实收金额 >= 应收金额 才允许已结算
```

金额必须使用 `BigDecimal`。

### 5.3 官方售后

官方结算和客户支付必须分开。

禁止：

```text
把官方结算金额加入客户支付金额
```

允许：

```text
客户支付收入单独统计
官方结算收入单独统计
财务报表中分别展示
```

### 5.4 报销

报销状态：

```text
待确认
已确认
已驳回/取消
```

只有已确认的报销才能进入运营成本。

### 5.5 字典配置

禁止硬编码：

- 工单状态
- 支付方式
- 配件来源
- 配件分类
- 角色
- 权限点
- 门店

MVP 可使用简单字典表，不要实现复杂配置平台。

## 6. Codex 禁止事项

Codex 不得：

- 擅自引入 Redis/MQ/微服务
- 擅自实现多门店正式能力
- 擅自修改财务口径
- 擅自改变库存预占时机
- 擅自改变已结算条件
- 擅自把官方结算与客户收款合并
- 擅自把报销提交直接计入成本
- 擅自删除审计字段
- 擅自大规模重构无关模块

## 7. Codex 任务启动模板

Owner 或 ChatGPT 给 Codex 的任务应使用：

```text
任务名称：

业务目标：

当前阶段：

涉及模块：

涉及表：

必须遵守的业务规则：

不做范围：

实现要求：

测试要求：

输出要求：
```

详见 `docs/TASK_CARD_TEMPLATE.md`。
