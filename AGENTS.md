# AI Agent 通用工作规范

本文件适用于所有参与本项目的 AI Agent，包括 Codex、Claude、Copilot 或其他代码助手。

## 1. 项目基本事实

本项目是“小牛电动官方授权店两轮车维修售后库存管理系统”。

当前阶段是多门店数据隔离的门店售后库存管理系统。门店是数据隔离边界，超管具备全局管理能力，门店管理员具备本门店管理能力；本项目不是大型 ERP，不是微服务系统。

核心业务链路：

```text
配件入库
  ↓
创建工单
  ↓
提交工单并预占库存
  ↓
客户支付 / 退款
  ↓
工单结算
  ↓
库存正式扣减
  ↓
官方结算 / 报销 / 财务统计
```

## 2. 所有 Agent 必须遵守的原则

### 2.1 不擅自扩大范围

不得主动实现以下内容：

- 复杂连锁/区域运营能力
- 跨门店自动调拨或自动结算
- 自动采购
- 同步官方系统
- 自动打款
- 员工提成
- 客户端小程序
- 图片上传
- 客户签字
- 复杂审批流
- 复杂盘点系统
- 微服务
- MQ
- Redis
- 大型权限平台
- 低代码配置平台

除非 Owner 明确下达任务。

### 2.2 不绕过业务规则

以下规则禁止绕过：

- 库存变化必须有库存流水
- 工单提交时才预占库存
- 工单结算后才正式扣减库存
- 工单取消必须释放预占库存
- 支付支持多次付款和混合付款
- 退款必须生成退款记录
- 已结算必须基于实收金额判断
- 官方结算与客户支付必须分开统计
- 报销确认后才计入运营成本
- 所有金额使用 `BigDecimal`
- 状态流转必须校验前置状态

### 2.3 不直接修改核心逻辑

未经明确任务，不得修改：

- 库存预占逻辑
- 库存扣减逻辑
- 库存释放逻辑
- 支付/退款计算逻辑
- 工单结算条件
- 官方结算统计逻辑
- 财务利润计算口径
- 权限模型
- 数据库主结构

### 2.4 必须输出变更说明

每次代码变更后，Agent 必须说明：

```text
1. 改了哪些文件
2. 为什么改
3. 是否涉及数据库变更
4. 是否涉及核心业务规则
5. 如何测试
6. 哪些内容没有做
7. 需要 Owner 注意什么
```

## 3. 风险分级

### HIGH

以下任务属于高风险，默认交给 Codex 或 Owner 审核：

- 数据库表结构变更
- 库存相关逻辑
- 工单状态机
- 支付退款结算
- 官方售后结算
- 财务报表口径
- 权限模型
- 事务一致性
- 大规模重构

### MEDIUM

可以由 Codex 或 Claude 执行，但需要任务卡：

- 新增普通接口
- 管理端列表页
- 小程序表单页
- Excel 导出
- 普通查询筛选
- 测试用例补充

### LOW

Claude 可以直接处理：

- 文案修改
- 字段展示
- 简单样式
- 表单校验
- mock 数据
- README / 文档同步
- 简单单元测试

## 4. 提交前自检

Agent 在提交前必须自查：

```text
是否扩大了 MVP 范围？
是否修改了核心规则？
是否缺少事务？
是否缺少库存流水？
是否把金额用了 double/float？
是否硬编码了状态/支付方式/角色？
是否缺少 store_id？
是否缺少操作人、操作时间？
是否缺少失败场景处理？
是否有测试或验收说明？
```

## 5. 禁止事项

禁止：

- 为了“更完整”擅自加复杂功能
- 为了“更高级”引入不必要技术
- 在未说明的情况下改变数据库结构
- 直接删除已有业务字段
- 把状态枚举散落在前后端多个地方
- 把库存改成简单 `stock = stock - quantity`
- 把支付直接塞进工单单字段
- 把官方结算算进客户支付
- 把报销提交直接计入成本
- 无任务卡进行大规模重构
