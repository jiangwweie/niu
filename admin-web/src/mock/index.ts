// Mock data index for the app
import { UserInfo } from '@/types';

export const mockUser: UserInfo = {
  username: '门店管理员',
  role: 'admin',
  storeId: 'S-001',
  storeName: '北京望京SOHO官方授权店',
};

// Will add mock functions here later instead of integrating mockjs which can be heavy.
// Example:
// export const getWorkOrders = () => { ... }
