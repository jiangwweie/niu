# M28 生产权限与搜索增强最终验收

## 1. 最终生产状态

- 验收日期：2026-06-08
- 生产 release：`20260608181056-16f54b060bbf`
- Flyway version：`15`
- backend：`active/enabled`
- 管理端：`https://admin.qytech.online`
- API：`https://api.qytech.online`
- 本轮状态：只新增文档，未改代码，未部署，未 push，未读取或输出任何密码、token、secret。

结论：M28 用户与权限管理已完成生产部署和管理端生产验收，不阻塞正式使用。

## 2. 权限验收结论

M28S Playwright 生产验收已通过：

| 验收对象 | 结论 |
| --- | --- |
| `SUPER_ADMIN` | 通过：可进入员工与权限，可查看用户列表、门店列、角色列，可创建测试用户、重置密码、启停测试用户，当前超管自我停用被阻止 |
| `STORE_ADMIN` | 通过：可进入员工与权限，只能看到本门店用户，新增用户时不能选择其他门店，角色下拉不包含 `SUPER_ADMIN`、`STORE_ADMIN` 或平台级角色 |
| 普通员工 / `TEST_STAFF` | 通过：可登录，看不到员工与权限入口，直接访问用户管理路由无管理入口，接口请求被拒绝 |
| 越权接口 | 通过：未登录返回 `401`，普通员工返回 `403`，`STORE_ADMIN` 跨店创建和分配 `SUPER_ADMIN` 均被业务拒绝 |

安全结论：

- 门店隔离由后端兜底。
- 角色分配不能通过前端传参越权。
- 自我禁用和最后超管保护在 M28R/M28S 中均已确认。
- 临时密码只在创建/重置弹窗中出现，本轮未输出或记录任何临时密码。
- 未删除历史数据，未修改真实业务用户。

## 3. 搜索验收结论

管理端搜索 smoke 已通过：

- 客户搜索
- 车辆搜索
- 工单搜索
- 配件搜索
- 库存搜索

结论：本轮未发现 M27B 搜索增强和 M28 权限管理部署破坏管理端搜索或主要查看流程。

小程序真机搜索 smoke 待 Owner 确认，范围包括：

- 客户搜索
- 车辆搜索
- 配件搜索
- 工单搜索
- 库存搜索

## 4. 测试用户处理建议

生产验收创建的测试用户：

| 用户名 | 创建来源 | 分配角色 | 建议 |
| --- | --- | --- | --- |
| `TEST_M28_SUPER_MQ52UKKB9448` | `SUPER_ADMIN` | `FINANCE` | 保留用于后续回归；如需清理，仅建议停用 |
| `TEST_M28_STORE_MQ52UYJKEE65` | `STORE_ADMIN` | `FINANCE` | 保留用于后续回归；如需清理，仅建议停用 |

处理原则：

- 不删除 `TEST_M28_*` 用户。
- 不删除历史数据。
- 不修改真实业务用户。
- 如 Owner 后续确认清理测试账号，建议只停用测试用户，保留审计和历史链路。

## 5. 未完成事项

- 小程序体验版仍需上传。
- 小程序真机搜索 smoke 待 Owner 确认。
- 微信解绑真实动作未在 M28S 中执行，因为测试用户未绑定微信；后续可准备已绑定微信的测试员工单独验证。
- M27B-3 LIKE 安全治理未完成，作为后续事项跟进：统一搜索输入规范、LIKE 转义、空白字符处理、特殊字符处理、分页筛选保持、前后端搜索口径一致性。

## 6. 最终结论

M28 用户与权限管理生产验收通过，M27B 管理端搜索增强生产 smoke 通过。

正式使用建议：

- 可以启用 M28 用户与权限管理能力。
- 小程序体验版上传和真机搜索验收完成前，小程序侧搜索增强仍标记为待 Owner 确认。
- M27B-3 不阻塞本次 M28 正式使用，但应作为后续安全与一致性治理事项排期。
