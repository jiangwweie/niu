# M28 用户与权限管理生产验收收口

## 1. 背景

- 收口日期：2026-06-08
- 生产 release：`20260608181056-16f54b060bbf`
- Flyway version：`15`
- 生产管理端：`https://admin.qytech.online`
- 生产 API：`https://api.qytech.online`
- 关联验收记录：`docs/dev/m28s-playwright-permission-smoke.md`

本轮收口只记录生产验收结论，未修改业务代码，未部署，未 push，未读取或输出任何密码、token、secret。

## 2. 生产验收结论

结论：M28 用户与权限管理生产验收通过，不阻塞正式使用。

已确认：

- M28 已部署到生产。
- backend 处于 `active/enabled`。
- Flyway 已执行到 V15。
- `SUPER_ADMIN`、`STORE_ADMIN`、`TEST_STAFF` 均完成生产 smoke。
- 越权接口校验通过。
- 未输出密码、token、secret。
- 未删除历史数据。
- 未修改真实业务用户。
- 未 push。

## 3. 权限管理验收摘要

| 角色 | 验收结论 |
| --- | --- |
| `SUPER_ADMIN` | 通过：可进入员工与权限，查看用户列表、门店列、角色列，创建测试用户，重置密码，启停测试用户，且不能停用自己 |
| `STORE_ADMIN` | 通过：可进入员工与权限，只能看到本门店用户，角色下拉不包含 `SUPER_ADMIN`、`STORE_ADMIN` 或平台级角色，可管理本门店测试普通员工，不能停用自己 |
| `TEST_STAFF` | 通过：可登录，看不到员工与权限入口，直接访问用户管理路由无创建入口，接口请求被拒绝 |

## 4. 越权校验摘要

| 场景 | 结果 |
| --- | --- |
| 未登录请求 `/api/admin/users` | 通过，返回 `401` |
| 普通员工请求 `/api/admin/users` | 通过，返回 `403` |
| `STORE_ADMIN` 尝试创建其他门店用户 | 通过，业务拒绝 |
| `STORE_ADMIN` 尝试分配 `SUPER_ADMIN` | 通过，业务拒绝 |

## 5. 搜索与主流程影响

管理端搜索 smoke 已通过：

- 客户搜索
- 车辆搜索
- 工单搜索
- 配件搜索
- 库存搜索

本轮未发现 M28 破坏现有管理端搜索和主要查看流程。

小程序体验版仍需上传并执行体验版验收，尤其是 M27B-1 高频模糊搜索相关的客户、车辆、配件、工单、库存搜索。

## 6. TEST 用户清单

| 用户名 | 创建来源 | 分配角色 | 当前处理建议 |
| --- | --- | --- | --- |
| `TEST_M28_SUPER_MQ52UKKB9448` | `SUPER_ADMIN` | `FINANCE` | 保留用于回归；如需清理，建议停用而非删除 |
| `TEST_M28_STORE_MQ52UYJKEE65` | `STORE_ADMIN` | `FINANCE` | 保留用于回归；如需清理，建议停用而非删除 |

处理原则：

- 不删除 `TEST_M28_*` 用户，避免影响审计链路和历史记录。
- 如 Owner 后续确认清理，仅建议停用测试用户。
- 不修改真实业务用户角色，不禁用真实业务用户。

## 7. 敏感信息与数据安全

已确认：

- 未输出任何密码、token、JWT、微信 secret、DB password 或一次性临时口令。
- 未截图一次性口令弹窗。
- 未读取生产密码、token 或 secret。
- 未手动修改生产数据库。
- 未删除历史数据。
- 未修改真实用户。
- 未 reload/restart Nginx。
- 未 restart backend。

## 8. 未完成与后续事项

后续事项：

- 小程序体验版仍需上传，并补充体验版 smoke。
- 微信解绑真实动作本轮未执行，因为测试用户未绑定微信；后续可准备已绑定微信的测试员工账号单独验收。
- M27B-3 作为后续搜索输入规范治理事项，建议统一治理前后端搜索输入规则、空白字符、特殊字符、分页筛选保持和大小写/模糊匹配口径。

## 9. 是否可正式使用

建议：可以正式使用 M28 用户与权限管理能力。

上线后注意：

- 保持 `TEST_M28_*` 测试账号可识别，不与真实用户混用。
- 后续新增角色定义写接口时，必须重新评估 `ROLE_MANAGE` 对 `STORE_ADMIN` 的授权范围。
- 后续小程序体验版上传后，应补齐小程序搜索 smoke 记录。
