# M28S 用户与权限管理 Playwright 生产验收

## 1. 验收时间

- 时间：2026-06-08 18:37 CST
- 执行方式：Playwright Chromium headless
- 执行范围：生产管理端 UI smoke + 浏览器上下文 API 越权辅助验证

## 2. 测试环境

- 管理端：`https://admin.qytech.online`
- API：`https://api.qytech.online`
- backend：已部署 M28，服务 `active/enabled`
- Flyway：已到 V15
- 本轮未 push、未 reload/restart Nginx、未 restart backend、未手动修改生产数据库

## 3. 使用的角色

- `SUPER_ADMIN`
- `STORE_ADMIN`
- `TEST_STAFF`

账号凭据从本地环境变量或 `.env.m28s.local` 读取，文档、脚本输出和提交内容均未记录密码、token、secret 或一次性临时口令。

## 4. SUPER_ADMIN 验收结果

| 验收项 | 结果 |
| --- | --- |
| 成功进入管理端 | 通过 |
| 可见“员工与权限”入口 | 通过 |
| 可进入用户管理页 | 通过 |
| 用户列表可见 | 通过 |
| 门店列可见 | 通过 |
| 角色列可见 | 通过 |
| 微信绑定状态列可见 | 通过 |
| 新增员工按钮可见 | 通过 |
| 新增用户弹窗可选择门店 | 通过 |
| 角色下拉可选择合法门店角色 | 通过 |
| 创建 `TEST_M28_*` 用户后出现一次性口令弹窗 | 通过，未输出口令 |
| 重置 `TEST_M28_*` 用户后出现一次性口令弹窗 | 通过，未输出口令 |
| 解绑微信 | 跳过，测试用户未绑定微信，页面无解绑入口 |
| 停用/启用 `TEST_M28_*` 用户 | 通过，最终已恢复启用 |
| 尝试停用当前超管自己 | 通过，页面无停用当前账号入口 |

## 5. STORE_ADMIN 验收结果

| 验收项 | 结果 |
| --- | --- |
| 成功进入管理端 | 通过 |
| 可见“员工与权限”入口 | 通过 |
| 用户列表仅包含本门店用户 | 通过 |
| 新增用户时无跨门店选择能力 | 通过 |
| 如显示门店，为固定当前门店 | 通过 |
| 角色下拉不包含 `SUPER_ADMIN` | 通过 |
| 角色下拉不包含 `STORE_ADMIN` | 通过 |
| 角色下拉不包含平台级角色 | 通过 |
| 可创建本门店 `TEST_M28_*` 普通员工 | 通过 |
| 可重置本门店 `TEST_M28_*` 普通员工 | 通过，未输出口令 |
| 解绑本门店 `TEST_M28_*` 普通员工微信 | 跳过，测试用户未绑定微信，页面无解绑入口 |
| 不能操作 `SUPER_ADMIN` | 通过，列表范围内无对应入口 |
| 不能操作其他门店用户 | 通过，列表范围内无其他门店用户 |
| 不能禁用自己 | 通过，页面无停用当前账号入口 |

## 6. 普通员工验收结果

使用 `STORE_ADMIN` 创建的 `TEST_M28_*` 普通员工完成登录验证。

| 验收项 | 结果 |
| --- | --- |
| 普通员工可登录 | 通过 |
| 普通员工看不到“员工与权限”入口 | 通过 |
| 直接访问用户管理路由 | 通过，未出现新增员工入口 |
| 不可见创建用户、角色分配、重置等入口 | 通过 |

浏览器上下文 API 辅助验证确认普通员工请求 `/api/admin/users` 返回 `403`。

## 7. 越权接口验证结果

| 场景 | 结果 |
| --- | --- |
| 未登录请求 `/api/admin/users` | 通过，返回 `401` |
| 普通员工请求 `/api/admin/users` | 通过，返回 `403` |
| `STORE_ADMIN` 尝试创建其他门店用户 | 通过，返回业务拒绝 |
| `STORE_ADMIN` 尝试分配 `SUPER_ADMIN` | 通过，返回业务拒绝 |

未输出 token，未把 token 写入文件。

## 8. 搜索 smoke 结果

| 页面 | 结果 |
| --- | --- |
| 管理端客户搜索 | 通过 |
| 管理端车辆搜索 | 通过 |
| 管理端工单搜索 | 通过 |
| 管理端配件搜索 | 通过 |
| 管理端库存搜索 | 通过 |

本轮为管理端生产 UI smoke，未执行小程序真机搜索验收。

## 9. 创建的 TEST 用户清单

| 用户名 | 创建角色 | 分配角色 |
| --- | --- | --- |
| `TEST_M28_SUPER_MQ52UKKB9448` | `SUPER_ADMIN` | `FINANCE` |
| `TEST_M28_STORE_MQ52UYJKEE65` | `STORE_ADMIN` | `FINANCE` |

说明：

- 以上均为 `TEST_M28_*` 测试用户。
- 未删除测试用户。
- 未禁用真实用户。
- `SUPER_ADMIN` 创建的测试用户执行过停用/启用验证，最终恢复启用。
- 为完成普通员工登录验证，`STORE_ADMIN` 创建的测试用户完成了首次改密流程；未记录或输出改密后的口令。

## 10. 数据与敏感信息说明

- 未删除历史数据。
- 未修改真实业务用户。
- 未修改真实用户角色。
- 未清空真实用户微信绑定。
- 未手动修改生产数据库。
- 未输出任何密码、token、JWT、微信 secret、DB password 或一次性临时口令。
- 未截图一次性口令弹窗。

## 11. 发现的问题

未发现阻塞正式使用的问题。

非阻塞说明：

- 新建的测试用户均未绑定微信，因此微信解绑 UI 只验证到“无绑定时无解绑入口”的保护状态，未执行真实解绑动作。
- 普通员工直接访问用户管理路由时，前端未展示新增员工等管理入口；后端 API 已返回 `403`，安全边界以后端拒绝为准。
- 本轮未做小程序真机搜索 smoke。

## 12. 是否阻塞正式使用

结论：不阻塞正式使用。

已覆盖：

- 平台超管用户管理能力。
- 门店管理员门店隔离和角色分配边界。
- 普通员工入口隐藏和接口拒绝。
- 创建、重置、启停的 UI 二次确认与一次性口令弹窗。
- 管理端客户、车辆、工单、配件、库存搜索 smoke。

## 13. 后续建议

- 保留 `TEST_M28_*` 测试账号用于后续回归；如需停用，建议由 Owner 明确确认后仅停用测试账号，不删除历史数据。
- 下次有已绑定微信的测试员工时，补一次真实微信解绑 UI smoke。
- 小程序搜索建议安排单独真机或开发者工具验收，覆盖客户、车辆、配件、工单、库存搜索。
